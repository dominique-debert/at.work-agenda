class Store {}

const store = new Store();
export default store;

//  import Store from "./store.js"; Sur chaque composant
// Pour set -> Store.maVariable = "valeur";
// Pour get -> let maValeur = Store.maVariable;
// Attention de ne pas faire Store = null;
